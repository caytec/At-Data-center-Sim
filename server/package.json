{
  "name": "@gigarack/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GigaRack leaderboard + market-demand backend. Zero runtime dependencies (built-in node:http + node:sqlite).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
