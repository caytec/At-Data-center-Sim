# Base URL of the GigaRack leaderboard/demand backend.
# Defaults to http://localhost:8787 if unset.
# In production, point this at your deployed server (Render/Railway/Fly/etc.).
VITE_API_URL=http://localhost:8787
